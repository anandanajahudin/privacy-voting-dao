import React from "react";
import ReactDOM from "react-dom/client";  // pastikan ini tanda minus biasa
import App from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
